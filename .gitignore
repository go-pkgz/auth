# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
.vscode
*.cov
Dockerfile
# ralph progress logs
progress-*.txt

# example build artifact, go build -race in _example names it after the dir
/_example/_example

# local GOCACHE/GOMODCACHE, never commit build caches
/.gocache/
/.gomodcache/
